<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <data noupdate="1"> 
        <!-- Healthmate push notification -->
        <record id="cron_push_healthmate_notifications" model="ir.cron">
            <field name="name">Push Healthmate Notifications</field>
            <field name="active" eval="False" />
            <field name="user_id" ref="base.user_root" />
            <field name="interval_number">1</field>
            <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).strftime('%Y-%m-%d 03:00:00')" />
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="doall">0</field>
            <field name="model_id" ref="model_push_notification"/>
            <field name="state">code</field>
            <field name="code">model.cron_push_heallthmate_notification()</field>
        </record>
    </data>
</odoo>
