<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!-- <template id="portal.portal_share_template">
            <div>
                <p>Dear <span t-esc="partner.name"/>,</p>
                
                <t t-if="record._name == 'account.move">
                    <p>Your invoice is due for payment: Kindly click the button below to proceed: </p>
                </t>
                <t t-else="record._name != 'account.move">
                    <p>You have been invited to access the following document:</p>
                </t>
                <br/>
                <a t-attf-href="#{share_link}" style="background-color: #875A7B; padding: 10px; text-decoration: none; color: #fff; border-radius: 5px; font-size: 12px;">
                    <strong>Open </strong>
                    <t t-if="record._name == 'account.move">
                        <strong>Link to proceed with payment </strong>
                    </t>
                    <t t-else="record._name != 'account.move">
                        <strong t-esc="record.display_name"/>
                    </t>
                </a><br/>
                <br/>
                <p t-if="note" t-esc="note"/>
            </div>
        </template> -->
        <function name = "write" model = "ir.model.data">
            <function name = "search" model = "ir.model.data">
                <value eval = "[('name', '=', 'email_template_edi_invoice'), ('module','=','account')] "/>
            </function>
            <value eval =" {'noupdate': False} "/>
        </function>


        <record id="account.email_template_edi_invoice" model="mail.template" >
            <field name="name">Invoice: Send by email:</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="email_from">${(object.invoice_user_id.email_formatted or user.email_formatted) |safe}</field>
            <field name="partner_to">${object.partner_id.id}</field>
            <field name="subject">${object.company_id.name} Invoice (Ref ${object.name or 'n/a'})</field>
            <field name="body_html" type="html">
                <div style="margin: 0px; padding: 0px;">
                    <p style="margin: 0px; padding: 0px; font-size: 13px;">
                        Dear
                        % if object.partner_id.parent_id:
                            ${object.partner_id.name} (${object.partner_id.parent_id.name}),
                        % else:
                            ${object.partner_id.name},
                        % endif
                        <br /><br />
                        Here is your
                        % if object.name:
                            invoice <strong>${object.name}</strong>
                        % else:
                            invoice
                        %endif
                        % if object.invoice_origin:
                            (with reference: ${object.invoice_origin})
                        % endif
                        amounting in <strong>${format_amount(object.amount_total, object.currency_id)}</strong>
                        from ${object.company_id.name}.
                        % if object.payment_state == 'paid':
                            This invoice is already paid.
                        % else:
                            Please remit payment at your earliest convenience using the link below<br /><br />
                            <a href="${object.share_link}" style="background-color: #875A7B; padding: 10px; text-decoration: none; color: #fff; border-radius: 5px; font-size: 12px;">
                                <strong>Click here to view Invoice and conveniently pay online</strong>
                            </a>
                            <br />
                        % endif
                        <br /><br />
                        Do not hesitate to contact us if you have any questions.
                    </p>
                </div>
            </field>
            <field name="report_template" ref="account.account_invoices"/>
            <field name="report_name">Invoice_${(object.name or '').replace('/','_')}${object.state == 'draft' and '_draft' or ''}</field>
            <field name="lang">${object.partner_id.lang}</field>
            <field name="auto_delete" eval="True"/>
        </record>
    </data>
</odoo>
