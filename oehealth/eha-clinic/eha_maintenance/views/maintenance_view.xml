<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record model="ir.ui.view" id="hr_equipment_view_inherited_form">
        <field name="name">maintenance.equipment.inherit</field>
        <field name="model">maintenance.equipment</field>
        <field name="inherit_id" ref="maintenance.hr_equipment_view_form"/>
        <field name="arch" type="xml">
            <field name="location" position="after">
                <field name="branch_id" string="Used in location" options="{'no_open': True,'no_create': True}" force_save="1"/>
            </field>
            <field name="location" position="replace">
                <field name="location" string="Used in location" invisible="1"/>
            </field>
        </field>
    </record>
</odoo>
