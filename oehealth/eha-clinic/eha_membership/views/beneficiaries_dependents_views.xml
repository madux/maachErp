<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data>

    <record id="view_beneficiaries_dependents_action" model="ir.actions.act_window">
        <field name="name">Beneficiaries and Dependents</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">['|', '|', ('membership_id', '!=', False), ('membership_category_id', '!=', False), ('membership_plan_id', '!=', False)]</field>
        <field name="context">{'edit': False, 'create': False}</field>
    </record>
    
    <menuitem
        id="beneficiaries_dependents_menu"
        name="Beneficiaries &amp; Dependents"
        sequence="10"
        action="view_beneficiaries_dependents_action"
        parent="eha_membership_menu_root"
        />    
    </data>

</odoo>
