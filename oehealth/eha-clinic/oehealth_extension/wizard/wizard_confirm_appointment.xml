<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="wizard_confirm_nitp_appt_form" model="ir.ui.view">
            <field name="name">wizard.confirm.nitp.appointment.form</field>
            <field name="model">wizard.confirm.nitp.appointment</field>

            <field name="arch" type="xml">
                <form string="Book Appointment">
                    <group>
                        <!--branch ID is added from the eha_multi_branch module-->
                        <field name="text" colspan="4" nolabel="1" readonly="1" />
                        <newline />
                    </group>
                    <group/>
                    <group>
                        <group>
                            <field name="cif_ids" invisible="1"/>
                            <field name="location" widget="radio" />
                    <field name="branch_id" attrs="{'invisible': [('location','=', 'others')], 'required': [('location','=', 'abuja-kano')]}" options="{'no_create': True, 'no_open': True}" />
                            <field name="date_appt" attrs="{'invisible': [('location', '=', 'abuja-kano')], 'required': [('location', '=', 'others')]}"  />
                        </group>
                        <group/>
                    </group>
            
                    <newline/>
                    <footer>
                        <button name="action_confirm" type="object" class="oe_highlight" string="Confirm" />
                        <button special="cancel" class="" string="Close" />
                    </footer>
                </form>
            </field>
        </record>

        <record id="wizard_confirm_nitp_appt_action" model="ir.actions.act_window">
            <field name="name">Confirm appointment</field>
            <field name="res_model">wizard.confirm.nitp.appointment</field>

            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo>
